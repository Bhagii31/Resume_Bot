# ── PDF reading ───────────────────────────────────────────
pdfplumber>=0.10.0       # extracts text from PDFs, preserving layout
                         # better than pypdf for resumes: handles multi-column,
                         # bold headers, and whitespace more accurately

# ── Embeddings ───────────────────────────────────────────
sentence-transformers>=2.7.0   # loads all-MiniLM-L6-v2, runs locally, no API key
                                # ~80MB download on first run, then cached

# ── Vector database ──────────────────────────────────────
chromadb>=0.5.0          # local persistent vector store
                         # no server setup needed — saves to disk automatically

# ── Utilities ────────────────────────────────────────────
numpy>=1.26.0            # vector math (normalisation, dot product)
tqdm>=4.66.0             # progress bars when embedding large batches

# ── LLM layer: for chat.py (Phase 3) ─────────────────────
anthropic>=0.40.0        # Claude API client — used by chat.py for generation
                         # needs ANTHROPIC_API_KEY (get one at console.anthropic.com)

# ── Web app: for app.py (Phase 4) ────────────────────────
fastapi>=0.110.0         # web framework serving the chat UI + /api/chat
uvicorn>=0.29.0          # ASGI server — run with: uvicorn app:app
